import { IfAuthorOrAdminDirective } from './if-author-or-admin.directive';

describe('IfAuthorOrAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new IfAuthorOrAdminDirective();
    expect(directive).toBeTruthy();
  });
});
